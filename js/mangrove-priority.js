(async function(){
  const version='20260813-dropdown',params=new URLSearchParams(location.search),requestedDistrict=params.get('district'),regency=params.get('regency')||'riau',regencies={riau:'Provinsi Riau',intervention:'7 Desa Intervensi YG',bengkalis:'Kabupaten Bengkalis',siak:'Kabupaten Siak',dumai:'Kota Dumai','rokan-hilir':'Kabupaten Rokan Hilir','kepulauan-meranti':'Kabupaten Kepulauan Meranti','indragiri-hilir':'Kabupaten Indragiri Hilir',pelalawan:'Kabupaten Pelalawan'},scopeSlug=regencies[regency]?regency:requestedDistrict==='bandar-laksamana'?'bandar-laksamana':'riau',prefix=scopeSlug==='intervention'?'mangrove-priority':`mangrove-priority-${scopeSlug}`,colors={P1:'#087b61',P2:'#41a66f',P3:'#258eb0',P4:'#d27b21',P5:'#647b73',X:'#9b3f3f',U:'#7753a6'};
  const style=document.createElement('style');style.textContent=`.region-switcher{display:grid;grid-template-columns:minmax(180px,240px) 1fr;gap:16px;align-items:stretch;margin:24px 0;padding:14px;background:#e7f2ee;border:1px solid var(--line);border-radius:20px}.region-switcher-heading{display:flex;flex-direction:column;justify-content:center;padding:4px 10px}.region-switcher-heading span{font-weight:800;font-size:1.05rem}.region-switcher-heading small{margin-top:5px;color:#58736d}.region-options{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.region-options a{position:relative;display:flex;min-width:0;flex-direction:column;padding:13px 15px;border:1px solid #bcd7cf;border-radius:14px;background:#ffffffc7;color:var(--ink);text-decoration:none}.region-options a.active{background:var(--green);border-color:var(--green);color:#fff}.region-options a.active:after{content:'Aktif';position:absolute;right:10px;top:10px;padding:3px 7px;border-radius:999px;background:#ffffff2f;font-size:.65rem;font-weight:800}.region-options span{font-size:.68rem;font-weight:800;letter-spacing:.1em;text-transform:uppercase;opacity:.72}.region-options strong{margin-top:3px}.region-options small{margin-top:5px;opacity:.75}.district-explorer{margin:16px 0 22px;padding:18px;border:1px solid var(--line);border-radius:18px;background:#fff}.district-head{display:flex;justify-content:space-between;align-items:end;gap:18px}.district-head h2{margin:2px 0}.district-head p{margin:0;color:#58736d}.district-options{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:9px;margin-top:14px}.district-card{padding:12px;border:1px solid var(--line);border-radius:12px;background:#f5faf8;color:var(--ink);text-align:left;cursor:pointer}.district-card.active{background:var(--green);border-color:var(--green);color:#fff}.district-card strong,.district-card small{display:block}.district-card small{margin-top:4px;opacity:.78}.scope-breadcrumb{display:flex;gap:6px;align-items:center;margin:0 0 10px;color:#58736d;font-size:.78rem}.scope-breadcrumb b{color:var(--green)}.district-group{margin:13px 0 5px;padding:7px 8px;border-radius:8px;background:#dfeee9;color:#315c54;font-size:.75rem;font-weight:900;letter-spacing:.05em;text-transform:uppercase}.district-group:first-child{margin-top:0}.village-list button.filtered-out{display:none}.class-methods{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px;margin:18px 0 28px}.class-methods article{padding:16px;border:1px solid var(--line);border-left:6px solid #647b73;border-radius:14px;background:#fff}.class-methods article[data-class=P1]{border-left-color:${colors.P1}}.class-methods article[data-class=P2]{border-left-color:${colors.P2}}.class-methods article[data-class=P3]{border-left-color:${colors.P3}}.class-methods article[data-class=P4]{border-left-color:${colors.P4}}.class-methods article[data-class=P5]{border-left-color:${colors.P5}}.class-methods article[data-class=X]{border-left-color:${colors.X}}.class-methods article[data-class=U]{border-left-color:${colors.U}}.class-methods article p{margin:.5rem 0 0;color:#47645e;line-height:1.55}@media(max-width:1050px){.region-switcher{grid-template-columns:1fr}.region-options{grid-template-columns:repeat(2,1fr)}}@media(max-width:700px){.region-options{grid-template-columns:1fr}.district-head{display:block}.district-options{grid-template-columns:1fr 1fr}}`;document.head.appendChild(style);
  const kpiStyle=document.createElement('style');kpiStyle.textContent='.kpis strong,.kpis small{display:block}.kpis strong{line-height:1.2;margin:5px 0}.region-switcher{grid-template-columns:minmax(180px,240px) minmax(0,1fr)}.region-options{display:block}.region-options select,.district-control select{width:100%;min-height:52px;padding:0 44px 0 16px;border:1px solid #a9cec4;border-radius:13px;background:#fff;color:var(--ink);font:inherit;font-weight:750;cursor:pointer}.district-head{align-items:center}.district-control{width:min(100%,560px)}.district-control p{margin:7px 2px 0!important;font-size:.82rem}.district-explorer{padding:15px 18px}.workspace{align-items:start}.workspace>.panel:first-child{height:620px;display:flex;flex-direction:column;overflow:hidden}.village-tools{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:10px}.village-tools label span{display:block;margin:0 0 4px;font-size:.7rem;font-weight:800;color:#58736d}.village-tools input,.village-tools select{width:100%;height:38px;min-width:0;padding:0 9px;border:1px solid var(--line);border-radius:9px;background:#fff;color:var(--ink)}.village-tools small{grid-column:1/-1;color:#58736d}.village-list{gap:6px;min-height:0;overflow-y:auto;padding-right:5px;scrollbar-gutter:stable}.village-list button{border-radius:10px;padding:8px 10px}.village-empty{padding:12px;color:#58736d;text-align:center}@media(max-width:700px){.district-head{display:grid;gap:12px}.district-control{width:100%}.region-options select,.district-control select{min-height:48px}.workspace>.panel:first-child{height:360px}.village-tools{position:sticky;top:0;background:#fff;z-index:2}}';document.head.appendChild(kpiStyle);
  const files=scopeSlug==='intervention'?['mangrove-priority-intervention.json','mangrove-priority-results.json','mangrove-priority-candidates.geojson']:[`${prefix}-villages.json`,`${prefix}-results.json`,`${prefix}-candidates.geojson`];
  const [foundation,results,candidates]=await Promise.all(files.map(file=>fetch(`data/${file}?v=${version}`).then(r=>{if(!r.ok)throw new Error(`${file}: ${r.status}`);return r.json()})));
  const regionOptions=document.querySelector('.region-options'),regionLinks=[...regionOptions.querySelectorAll('a')],regionSelect=document.createElement('select');regionSelect.id='region-select';regionSelect.setAttribute('aria-label','Pilih wilayah analisis');regionSelect.innerHTML=regionLinks.map(link=>`<option value="${link.getAttribute('href')}">${link.querySelector('strong').textContent} — ${link.querySelector('small').textContent}</option>`).join('');regionOptions.replaceChildren(regionSelect);regionSelect.value=regionLinks.find(link=>link.dataset.region===regency)?.getAttribute('href')||'mangrove-priority.html';regionSelect.addEventListener('change',()=>location.href=regionSelect.value);
  const activeRegion=regencies[regency]?regency:(scopeSlug==='bandar-laksamana'?'bengkalis':null);document.querySelectorAll('.region-options a').forEach(link=>{const active=link.dataset.region===activeRegion;link.classList.toggle('active',active);if(active)link.setAttribute('aria-current','page')});
  if(scopeSlug){const name=regencies[scopeSlug]||'Kecamatan Bandar Laksamana',scopeLabel=scopeSlug==='riau'?'PROVINSI':scopeSlug==='intervention'?'PROGRAM YG':regencies[scopeSlug]?'KABUPATEN/KOTA':'KECAMATAN';document.querySelector('.hero .eyebrow').textContent=`${scopeLabel} SELESAI · ${name.toUpperCase()} · KONDISI TERBARU 2026`;document.querySelector('.hero h1').textContent=`Prioritas mangrove ${name}`;document.querySelector('.panel .eyebrow').textContent='PILIH DESA PESISIR';document.querySelector('.panel h2').textContent=name;document.querySelector('.hero a[href$="geojson"]').href=`data/${prefix}-candidates.geojson`;document.querySelector('.hero a[href$="csv"]').href=`data/${prefix}-ranking.csv`;document.getElementById('district-result-link').hidden=true;document.getElementById('analysed-scope').textContent=`desa pesisir ${name}`}
  const villages=foundation.villages,records=new Map(results.villages.map(v=>[v.id,v])),districtKey=v=>v.districtKey||v.district,districts=[...new Set(villages.map(districtKey))].sort((a,b)=>a.localeCompare(b,'id'));let activeDistrict=districts.includes(requestedDistrict)?requestedDistrict:'';
  const map=L.map('priority-map').setView([1.42,102.08],9),imagery=L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{attribution:'Tiles © Esri'}).addTo(map),streets=L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap contributors'});L.control.layers({'Citra satelit':imagery,'Peta jalan':streets},null,{collapsed:false}).addTo(map);
  const candidateLayer=L.geoJSON(candidates,{style:f=>({color:colors[f.properties.priorityClass]||'#555',weight:2,fillColor:colors[f.properties.priorityClass]||'#777',fillOpacity:.42}),onEachFeature:(f,l)=>{const p=f.properties;l.bindPopup(`<strong>${p.priorityClass} · ${p.priorityLabel}</strong><br>${p.village} · ${p.areaHa.toFixed(3)} ha<br>Prioritas ${p.priorityScore}/100 · peringkat desa #${p.villageRank}<br>Kebutuhan ${p.needScore} · kelayakan ${p.suitabilityScore} · risiko ${p.riskScore}<br>${p.decisionReason}<br><small>${p.polygonId}</small>`)}}).addTo(map),polygonLayers=[];candidateLayer.eachLayer(l=>polygonLayers.push(l));
  const layers=new Map();polygonLayers.forEach(l=>{const id=l.feature.properties.id;if(!layers.has(id))layers.set(id,[]);layers.get(id).push(l)});const markers=new Map(),list=document.getElementById('village-list'),fmt=n=>Number.isFinite(n)?n.toFixed(2):'—';let villageQuery='',villageSort='name',selectedVillageId='';const villageTools=document.createElement('div');villageTools.className='village-tools';villageTools.innerHTML='<label><span>Cari desa</span><input id="village-search" type="search" placeholder="Ketik nama desa…" autocomplete="off"></label><label><span>Urutkan</span><select id="village-sort"><option value="name">Nama A–Z</option><option value="p1">P1 terbanyak</option><option value="area">Luas terbesar</option><option value="score">Skor tertinggi</option></select></label><small id="village-visible-count"></small>';list.before(villageTools);
  const explorer=document.createElement('section');explorer.className='district-explorer';explorer.innerHTML='<div class="district-head"><div><p class="eyebrow">KLASTER KECAMATAN</p><h2>Pilih kecamatan</h2></div><div class="district-control"><select id="district-select" aria-label="Pilih kecamatan"></select><p id="district-summary">KPI, daftar desa, dan polygon mengikuti wilayah terpilih.</p></div></div>';document.querySelector('.kpis').before(explorer);
  const breadcrumb=document.createElement('div');breadcrumb.className='scope-breadcrumb';document.querySelector('.panel.detail').prepend(breadcrumb);
  function districtStats(name){const selected=villages.filter(v=>!name||districtKey(v)===name),ids=new Set(selected.map(v=>v.id)),features=candidates.features.filter(f=>ids.has(f.properties.id));return {villages:selected,ids,features,area:features.reduce((s,f)=>s+(f.properties.areaHa||0),0),p1:features.filter(f=>f.properties.priorityClass==='P1').length}}
  function districtLabel(key){const sample=villages.find(v=>districtKey(v)===key);return sample&&sample.districtKey?`${sample.regency} · ${sample.district}`:key}
  function renderDistrictOptions(){const root=document.getElementById('district-select');root.innerHTML='<option value="">Semua kecamatan</option>'+districts.map(name=>`<option value="${name}">${districtLabel(name)}</option>`).join('');root.value=activeDistrict}
  function select(v){selectedVillageId=v.id;const r=records.get(v.id),group=layers.get(v.id)||[],top=group.map(l=>l.feature.properties).sort((a,b)=>a.villageRank-b.villageRank)[0],limited=r.status==='insufficient-data';document.querySelectorAll('.village-list button').forEach(b=>b.classList.toggle('active',b.dataset.id===v.id));document.querySelector(`.village-list button[data-id="${CSS.escape(v.id)}"]`)?.scrollIntoView({block:'nearest'});breadcrumb.innerHTML=`<span>${regencies[regency]||v.regency}</span><span>›</span><b>${v.district}</b><span>›</span><strong>${v.village}</strong>`;document.getElementById('detail-name').textContent=v.village;document.getElementById('detail-meta').textContent=`${v.district} · ${v.regency}`;document.getElementById('analysis-status').textContent=limited?'Data tidak memadai':`Dianalisis · keyakinan ${r.confidence}`;document.getElementById('need-score').textContent=limited?'—':`${r.needScore}/100 · ${r.needClass}`;document.getElementById('suitability-score').textContent=limited?'—':`${r.suitabilityScore}/100 · ${r.suitabilityClass}`;document.getElementById('recommended-action').textContent=limited?'Belum dinilai; citra bersih belum mencukupi.':r.recommendedAction;document.getElementById('candidate-area').textContent=`${fmt(r.priorityAreaHa)} ha`;document.getElementById('mangrove-area').textContent=limited?'—':`${fmt(r.baselineMangroveHa)} / ${fmt(r.currentMangroveHa)} ha`;document.getElementById('loss-area').textContent=limited?'—':`${fmt(r.indicativeMangroveLossHa)} ha`;document.getElementById('confidence').textContent=limited?'tidak memadai':`${r.confidence} · bebas awan ${r.clearCoveragePct}%`;document.getElementById('top-priority').textContent=top?`${top.priorityClass} · ${top.priorityScore}/100`:'—';if(group.length)map.fitBounds(L.featureGroup(group).getBounds(),{padding:[30,30],maxZoom:14});else map.setView([v.lat,v.lon],12);markers.get(v.id)?.openPopup()}
  function villageP1(v){return (records.get(v.id)?.priorityClasses?.P1)||0}function villageTop(v){return records.get(v.id)?.topPriorityScore||0}function renderVillages(){const query=villageQuery.trim().toLocaleLowerCase('id'),shown=villages.filter(v=>(!activeDistrict||districtKey(v)===activeDistrict)&&(!query||`${v.village} ${v.district} ${v.regency}`.toLocaleLowerCase('id').includes(query))).sort((a,b)=>villageSort==='area'?(records.get(b.id).priorityAreaHa-records.get(a.id).priorityAreaHa):villageSort==='p1'?(villageP1(b)-villageP1(a)||a.village.localeCompare(b.village,'id')):villageSort==='score'?(villageTop(b)-villageTop(a)||a.village.localeCompare(b.village,'id')):a.village.localeCompare(b.village,'id'));list.replaceChildren();shown.forEach(v=>{const r=records.get(v.id),button=document.createElement('button');button.dataset.id=v.id;button.classList.toggle('active',v.id===selectedVillageId);button.innerHTML=`<strong>${v.village}</strong><small>${r.priorityPolygonCount} polygon · ${r.priorityAreaHa.toFixed(2)} ha · ${villageP1(v)} P1</small>`;button.onclick=()=>select(v);list.appendChild(button)});document.getElementById('village-visible-count').textContent=`${shown.length} desa ditampilkan`;if(!shown.length){const empty=document.createElement('p');empty.className='village-empty';empty.textContent='Desa tidak ditemukan.';list.appendChild(empty)}}
  villages.forEach(v=>{const r=records.get(v.id),marker=L.circleMarker([v.lat,v.lon],{radius:7,color:'#fff',weight:2,fillColor:'#087b61',fillOpacity:1}).addTo(map).bindPopup(`<strong>${v.village}</strong><br>${r.priorityPolygonCount} polygon prioritas`);markers.set(v.id,marker)});document.getElementById('village-search').addEventListener('input',e=>{villageQuery=e.target.value;renderVillages()});document.getElementById('village-sort').addEventListener('change',e=>{villageSort=e.target.value;renderVillages()});
  function applyDistrict(name){activeDistrict=name;const s=districtStats(name);document.getElementById('district-select').value=name;document.getElementById('district-summary').textContent=`${s.villages.length} desa/kelurahan · ${s.features.length} polygon · ${s.p1} P1 · ${s.area.toFixed(2)} ha`;renderVillages();polygonLayers.forEach(l=>{const show=s.ids.has(l.feature.properties.id),present=candidateLayer.hasLayer(l);if(show&&!present)candidateLayer.addLayer(l);if(!show&&present)candidateLayer.removeLayer(l)});markers.forEach((m,id)=>{const show=s.ids.has(id),present=map.hasLayer(m);if(show&&!present)m.addTo(map);if(!show&&present)m.remove()});document.getElementById('analysed-count').textContent=s.villages.length;document.getElementById('polygon-count').textContent=s.features.length;document.getElementById('candidate-total').textContent=`${s.area.toFixed(2)} ha`;document.getElementById('analysed-scope').textContent=name?`desa/kelurahan ${districtLabel(name)}`:`desa pesisir ${regencies[regency]||''}`;const url=new URL(location.href);if(name)url.searchParams.set('district',name);else url.searchParams.delete('district');history.replaceState(null,'',url);if(s.features.length&&candidateLayer.getBounds().isValid())map.fitBounds(candidateLayer.getBounds(),{padding:[25,25],maxZoom:11});if(s.villages.length)select(s.villages[0])}
  document.getElementById('district-select').addEventListener('change',e=>applyDistrict(e.target.value));renderDistrictOptions();applyDistrict(activeDistrict);
  const classCounts={};candidates.features.forEach(f=>classCounts[f.properties.priorityClass]=(classCounts[f.properties.priorityClass]||0)+1);document.getElementById('page-status').textContent=`Prioritas selesai · ${candidates.features.length} polygon`;const labels={P1:'Penanaman aktif',P2:'Regenerasi alami',P3:'Pemulihan hidrologi',P4:'Perlindungan pantai',P5:'Perlindungan mangrove',X:'Tidak direkomendasikan',U:'Data belum memadai'};document.getElementById('priority-legend').innerHTML=Object.keys(labels).map(k=>`<span style="display:inline-block;margin:4px;padding:7px 10px;border-radius:999px;background:${colors[k]};color:white;font-weight:700">${k} ${labels[k]} · ${classCounts[k]||0}</span>`).join('');
})().catch(error=>{document.getElementById('page-status').textContent='Data prioritas gagal dimuat';console.error(error)});
